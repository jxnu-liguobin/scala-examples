package UnitTesting

package object Controllers {
    /**
     * Since your controllers are just regular classes, you can easily unit test them using Play helpers.
     * If your controllers depends on another classes, using dependency injection will enable you to mock these dependencies.
     */
}
